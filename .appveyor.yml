environment:
  matrix:
    - PYTHON: "C:\\Python35-x64"

install:
  - "%PYTHON%\\python.exe -m pip install wheel pylint opencv-python numpy pytest-cov pillow"

build: off

test_script:
  - "%PYTHON%\\python.exe colorscope.py -h"
    #  - "%PYTHON%\\pytest.exe colorscope.py -h"
  - "%PYTHON%\\python.exe colorscope.py -h"
